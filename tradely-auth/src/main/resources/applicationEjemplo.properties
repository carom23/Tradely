# Servidor
server.port=8081

# db PostgreSQL
spring.datasource.url=jdbc:postgresql://localhost:5432/tradely
spring.datasource.username=postgres
spring.datasource.password=password

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# propiedades mapeadas con @Value en JWTService
app.jwt.secret=un_secreto_muy_largo_de_al_menos_32_caracteres_para_HMAC
app.jwt.access-expiration-ms=900000
app.jwt.refresh-expiration-ms=604800000

# URL del agent-service
app.agent-service.url=http://localhost:3001
# Hyperliquid API URL
hyperliquid.api.url=https://api.hyperliquid.xyz
